# AI-OS — Delivery Copilot (v0)

A CLI tool that generates delivery docs (Mini-Spec, Backlog, Status) from a single YAML intake file.

## Requirements
- Node 20 (recommended via nvm)
- npm

## Setup
```bash
nvm use
npm install

cat > README.md <<'EOF'
# AI-OS — Delivery Copilot (v0)

A CLI tool that generates delivery docs (Mini-Spec, Backlog, Status) from a single YAML intake file.

## Requirements
- Node 20 (recommended via nvm)
- npm

## Setup
```bash
nvm use
npm install

